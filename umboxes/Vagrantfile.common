# encoding: utf-8
# -*- mode: ruby -*-
# vi: set ft=ruby :

# Loads baseline Vagrantfile first.
load File.expand_path('../baseline/Vagrantfile', __FILE__)

# All Vagrant configuration is done below.
Vagrant.configure(2) do |config|

  config.vm.hostname = VM_NAME

end